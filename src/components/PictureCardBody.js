import React from 'react'
import Button from 'bootstrap/js/dist/button';

const PictureCardBody = ({title, text, url}) => {  
  return (
    <div className="card-body">
        {/* <h5 className="card-title">{title.FirstName}{title.LastName}</h5>
        <p className="card-text">{text.Position} {text.College} {text.BirthCity},{text.BirthState} {text.Experience}</p>
        <Button url={url.NbaDotComPlayerID}> /> */}
      </div>
  );
};

//export the component
export default PictureCardBody